```python
class Myclass:
  # 定义构造方法
  def __init__(self, first, second):
    self.f = first
    self.s = second

  i = 123
  __privateattr = 'pp'  # 私有属性
  def memb(self):  # 类方法必须包含self参数
    return 'hello'

x = Myclass(1, 2)
print(x.i)
# print(x.__privateattr)
print(x.memb())
print(x.f, x.s)
```
