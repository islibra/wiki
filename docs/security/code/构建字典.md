```python
import string

s = string.digits + string.ascii_uppercase  # 数字和大写字母的列表
# 构建4位字典
f = open('dict.txt', 'w')
for i in s:
  for j in s:
    for p in s:
      for q in s:
        f.write(i + j + p + q + '\n')
f.close()
```
